server.port: 8761
spring.application.name: bug-wars-eureka
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://user:${eureka.user.password}@${eureka.instance.hostname}:${server.port}/eureka